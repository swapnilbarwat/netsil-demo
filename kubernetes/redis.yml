apiVersion: v1
kind: Deployment
metadata:
  name: redis
spec:
  replicas: 3
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
      - name: redis
        image: redis
        ports:
        - containerPort: 6379